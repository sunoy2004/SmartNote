services:
  - type: web
    name: smartnote-backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "python backend/main.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
    healthCheckPath: /health